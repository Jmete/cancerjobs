# Optional public API base URL override.
# Leave empty to use same-origin Next.js routes (/api/*).
NEXT_PUBLIC_API_BASE_URL=

# Local SQLite configuration
LOCAL_SQLITE_PATH=data/cancerjobs.sqlite

# Server-side admin API token used for secure admin routes (do not expose in client)
ADMIN_API_TOKEN=replace-with-random-admin-api-token

# Better Auth secret for signing/encrypting auth state (minimum 32 chars).
BETTER_AUTH_SECRET=replace-with-random-32-plus-char-secret

# Optional base URL used by CLI scripts like csv:upload and ops:health-check
API_URL=http://localhost:3002

# Optional trusted origins for Better Auth callbacks/session requests.
# Example: https://cancerjobs.com,https://www.cancerjobs.com
BETTER_AUTH_TRUSTED_ORIGINS=

# Optional comma-separated allowlist for admin mutation origins.
# Example: https://cancerjobs.com,https://www.cancerjobs.com
ADMIN_ALLOWED_ORIGINS=

# Optional for ops:health-check script alerts
ALERT_WEBHOOK_URL=
